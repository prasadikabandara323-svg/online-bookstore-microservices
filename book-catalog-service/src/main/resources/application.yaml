server:
  port: 8082

spring:
  application:
    name: book-catalog-service
  mongodb:
    uri: ${MONGODB_URI:mongodb://localhost:27017/bookstore_db}

app:
  api:
    key: ${API_KEY:YOUR_SECRET_API_KEY_HERE}